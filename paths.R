# Overall directory to be used for the workflow after initial Data preparation
# e.g. for file output and figures etc.
project_dir <- ""

# directory path pointing to where the raw FTIR and concentration data is stored: the rawdata folder in this repo
Input_dir <- "rawdata"

# directory where the output is stored. Should be a subdirectory of project_dir
output_dir <- "Output/"
